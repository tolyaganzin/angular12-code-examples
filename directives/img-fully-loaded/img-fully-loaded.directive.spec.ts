import { ImgFullyLoadedDirective } from './img-fully-loaded.directive';

describe('ImgFullyLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgFullyLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
